import React from "react";
import ReactDOM from "react-dom/client";
import Torch from "./assets/Components/Torch/Torch";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <Torch />
  </React.StrictMode>
);
